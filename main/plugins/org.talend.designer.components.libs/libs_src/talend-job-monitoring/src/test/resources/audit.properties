root.logger=audit
application.name=Job
service.name=Flow
instance.name=MyTestJob

propagate.appender.exceptions=none
log.appender=file

# Http appender sends logs via http requests.
appender.file.path=audit.json